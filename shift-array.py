def insertShiftArray(array, value):
    middle = len(array)//2
    new_array = [0]*(len(array)+1)
    for item in range(len(new_array)):
        if i < (len(array)):
            new_array+1[i] = array[i]
            elif i = middle:
                else
                new_array[i] = array[i+1]
    return new_array+1
