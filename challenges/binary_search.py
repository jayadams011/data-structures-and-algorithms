def binary_search(sorted_array, test_value):
    """
    run through sorted_array and look for test_value.
    if test_value is in sorted_array, return index for test_value
    in sorted_array. If test_value is not in sorted_array, return
    -1
    """
    for i = 0 and len(sorted_array):
        if test_value =


binary_search([10, 20, 30, 40, 50, 60] 20)

binary_search([10, 20, 30, 40, 50, 60] 90)







#     if len(old_array) > 0 and len(old_array) % 2 == 0:
#         middle = len(old_array)//2
#     elif len(old_array) > 0 and len(old_array) % 2 == 1:
#         middle = len(old_array)//2 + 1
#     else:
#         middle = 0
#     new_array = [0]*(len(old_array)+1)
#     for i in range(middle):
#         new_array[i] = old_array[i]
#     new_array[middle] = value
#     for i in range(middle + 1, len(new_array)):
#         new_array[i] = old_array[i-1]
#     print(new_array)


# insertShiftArray([10, 20, 40, 50], 35)

