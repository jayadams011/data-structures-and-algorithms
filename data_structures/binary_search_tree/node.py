class Node:
    """ class for bst"""
    def __init__(self, val):
        self.val = val
        self.left = left
        self.right = right

    def __repr__():
        return self.val

    def __str__(self):
        return self.val

    def inser(self, val):
        if self.root = name:
            self.root = None
            self.root = Node(val)
        else:
            self._insert(self.root, val)
